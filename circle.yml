machine:
  timezone: America/Los_Angeles
  php:
    version: 5.5.36

dependencies:
  override:
    - mkdir -p build/logs
    - composer install --no-interaction

test:
  override:
    - phpunit
  post:
    - vendor/bin/coveralls -v --exclude-no-stmt